__pycache__/
*.pyc
*.pyo
*.pyd
.vscode/
.DS_Store
*.code-workspace

# Local Codex/debug packaging artifacts
.pkgtest/
.piptmp/
.wheelcheck/
.wheel-test/
tmp*/

# Assistant / IDE workspace state
.claude/
.idea/

# Local AI / agent memory — never committed.
CLAUDE.md
CLAUDE.local.md

# Pytest cache
.pytest_cache/

# Leftover standalone config files produced by tests that fail mid-run
.standalone-test-*
.standalone_config-*.tmp

# Ephemeral local config and virtualenvs
.env
.venv/
venv/
# Root-anchor the Python packaging dirs so they don't accidentally
# match nested directories with the same name (notably the Vite
# bundle output at ``racelink/static/dist/``, which is a tracked
# build artefact that operators install without a Node toolchain).
/build/
/dist/
*.egg-info/
